import UploadClient from './UploadClient';

export const metadata = {
  title: 'Upload Trial Balance | Stochos Connected Reporting',
};

export default function UploadPage() {
  return <UploadClient />;
}
