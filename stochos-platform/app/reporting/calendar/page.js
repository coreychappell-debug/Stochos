import CalendarClient from './CalendarClient';

export const metadata = {
  title: 'Reporting Calendar | Stochos Platform',
};

export default function CalendarPage() {
  return <CalendarClient />;
}
