import WorkflowClient from './WorkflowClient';

export const metadata = {
  title: 'Workflow & Binders | Stochos Platform',
};

export default function WorkflowPage() {
  return <WorkflowClient />;
}
