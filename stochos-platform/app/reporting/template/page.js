import TemplateClient from './TemplateClient';

export const metadata = {
  title: 'Report Template Builder | Stochos Connected Reporting',
};

export default function TemplatePage() {
  return <TemplateClient />;
}
