import ReportingClient from './ReportingClient';

export const metadata = {
  title: 'Connected Reporting | Stochos Platform',
};

export default function ReportingPage() {
  return <ReportingClient />;
}
